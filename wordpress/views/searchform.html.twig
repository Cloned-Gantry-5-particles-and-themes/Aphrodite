{% set twigTemplate = 'searchform.html.twig' %}
{% set scope = 'searchform' %}

{% block searchform %}

    <form role="search" method="get" class="search-form form" action="{{ site.url }}/">
        <label>
            <span class="screen-reader-text">{{ __('Search for:', 'g5_callisto') }}</span>
            <input type="search" class="search-field" placeholder="{{ __( 'Search …', 'g5_callisto' ) }}" value="" name="s" title="{{ __('Search for:', 'g5_callisto') }}" />
        </label>
        <input type="submit" class="search-submit button button-4" value="Search" />
    </form>

{% endblock %}
